import React from 'react'

// 🐨 import the useAuth hook from /context/auth-context
// 🐨 import the AuthenticatedApp and UnauthenticatedApp components

function App() {
  // 🦉 you'll need to determine whether to render the AuthenticatedApp or the
  // UnauthenticatedApp based on whether the AuthProvider has user data
  // 🐨 call the useAuth custom hook to get the user
  // 🐨 if there's a user, render the AuthenticatedApp
  // 🐨 if there's not a user, render the UnauthenticatedApp
  return <div>app.js TODO</div>
}

export default App
