/** @jsx jsx */
import {jsx} from '@emotion/core'

// 🐨 you'll need the useAuth hook in /context/auth-context

function AuthenticatedApp() {
  // 🐨 take the username display and logout button that we had in the
  // unauthenticated app and put it here now that we're splitting that up.
  return <div>authenticated-app.js TODO</div>
}

export default AuthenticatedApp
// const Finished = require('./authenticated-app.finished.js')
// export default Finished.default
